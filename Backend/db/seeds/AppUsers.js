const bcrypt = require('bcrypt');

exports.seed = async function (knex) {
  // No-op: skipping test mobile user seed because gyms are added via Postman
  console.log('ℹ️ Skipping users seed: no users inserted.');
};
